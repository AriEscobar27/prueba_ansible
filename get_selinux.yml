---
- name: Selinux State
  hosts: all
  tasks:

  - name: Get Selinux
    ansible.builtin.shell: getenforce
    register: selinux_state

  - name: Print Selinux
    debug:
      msg: "{{ hostname }} {{ selinux }}"

